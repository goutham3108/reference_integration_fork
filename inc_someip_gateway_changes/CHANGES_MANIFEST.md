# inc_someip_gateway — local changes vs eclipse-score/inc_someip_gateway

Source: `/home/goutham/Gitrepos/SDV-Hackathon/inc_someip_gateway` (branch `rpi`)
Compared against: `eclipse-score/inc_someip_gateway` main @ 003aeae0 (merge-base f701652c)

Contains every file that is modified or newly added relative to the public
repo — i.e. the `rpi` branch commit ("Add RPI support and SDV runtime
submodule") plus all current uncommitted working-tree changes, with
directory structure preserved. 123 files total.

## Not included

- `sdv-runtime-fork` — this is a **git submodule** added on the `rpi` branch
  (pointer only, no regular file content to copy). If you need it, add the
  submodule separately in the target repo; see `.gitmodules` in this folder
  for its URL/path.
